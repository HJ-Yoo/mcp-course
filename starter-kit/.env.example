# Internal Ops Assistant — Environment Variables
# Copy this file to .env and fill in the values

# Server transport: "stdio" or "streamable-http"
TRANSPORT=stdio

# Host and port for streamable-http transport
HOST=0.0.0.0
PORT=8000

# Path to the data directory (defaults to ./data)
# DATA_DIR=./data

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# LLM API Keys (for Gradio UI demo — enter the key for your preferred provider)
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
